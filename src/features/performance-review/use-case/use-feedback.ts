// import { useDefaultList } from "@/components/use-cases";

// export function useGetFeedBack(review_id: number) {
//   const { data, ...rest } = useDefaultList();
//   // review_id ? REVIEW + `/${review_id}/feedback` : null
//   const feedback = data?.data;
//   return { feedback, ...rest };
// }
